<div class="container-fluid">
  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title">Subredes de <%= link_to @section.name, section_path(@section) %></h3>
    </div>
    <div class="panel-body">
      <%= link_to icon("arrow-left", "Secciones"), sections_path, class: "btn btn-warning" %>
      <div class="table-responsive">
        <table class="table" id="datatable">
          <thead>
            <tr>
              <th>Subred</th>
              <th>Descripción</th>
              <th>En Uso</th>
              <th>Disponibilidad</th>
              <th>Acciones</th>
            </tr>
          </thead>

          <tbody>
            <% @subnets.each do |subnet| %>
              <tr>
                <td><%= link_to subnet.cidr, subnet_addresses_path(subnet) %></td>
                <td><%= subnet.description %></td>
                <td><%= two_decimal_amount subnet.used_percentage %>%</td>
                <td><%= two_decimal_amount subnet.free_percentage %>%</td>
                <td>
                  <%= link_to(icon('search'), subnet_path(subnet)) %>
                  <%= link_to(icon('list'), subnet_addresses_path(subnet)) %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
